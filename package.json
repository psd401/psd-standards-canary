{
  "name": "psd-standards-canary",
  "version": "1.0.0",
  "private": true,
  "description": "Canary repo proving the PSD org enforcement stack. Safe to break on purpose.",
  "scripts": {
    "build": "node -e \"console.log('build ok')\"",
    "test": "node --test"
  }
}